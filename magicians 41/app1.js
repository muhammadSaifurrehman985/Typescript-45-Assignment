"use strict";
let magicians = ["Paradox", "Jado", "Cedula"];
function show_magicians() {
    for (let i = 0; i < magicians.length; i++) {
        const element = magicians[i];
        console.log(element);
    }
}
show_magicians();
